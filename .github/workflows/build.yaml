---
name: Build PKGBUILDs
on:
  pull_request:
    branches:
      - main
  schedule:
    - cron: "05 10 * * *" # 10:05am UTC every day
  push:
    branches:
      - main
    paths-ignore:
      - "**/README.md"
  workflow_dispatch:

jobs:
  build:
    name: Build PKGBUILDs
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Build PKGBUILDS
        uses: heckbat/archlinux-pkgbuilds@dd592d95be9d6ae5faf94dd85702a0a9febe3321
